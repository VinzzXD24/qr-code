<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HDR Converter</title>
    <style>
        body { font-family: Arial, sans-serif; max-width: 800px; margin: 20px auto; padding: 20px; }
        .upload-box { border: 2px dashed #ccc; padding: 30px; text-align: center; margin: 20px 0; }
        .error { color: red; margin: 10px 0; }
        .result-img { margin-top: 20px; border: 1px solid #ddd; padding: 10px; }
    </style>
</head>
<body>
    <h1>🖼️ HDR Image Converter</h1>
    
    <% if (error) { %>
        <div class="error"><%= error %></div>
    <% } %>

    <form action="/process" method="post" enctype="multipart/form-data">
        <div class="upload-box">
            <input type="file" name="image" accept="image/*" required>
            <p>Seret gambar atau klik untuk upload (Maks. 4MB)</p>
        </div>
        <button type="submit">Proses HDR</button>
    </form>

    <% if (resultUrl) { %>
        <div class="result-img">
            <h2>Hasil HDR:</h2>
            <img src="<%= resultUrl %>" alt="HDR Result" style="max-width: 100%;">
        </div>
    <% } %>

    <footer style="margin-top: 40px; color: #666;">
        <p>⚠️ Catatan:</p>
        <ul>
            <li>Proses membutuhkan waktu 10-30 detik</li>
            <li>Gambar hasil akan lebih besar ukurannya</li>
            <li>Hasil mungkin berbeda tergantung kualitas awal</li>
        </ul>
    </footer>
</body>
</html>
